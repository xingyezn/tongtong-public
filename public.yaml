# Public-safe endpoint placeholders only. Real deployment addresses, credentials,
# passwords, and device tokens belong in private/local.yaml and are never committed.

firmware:
  ota_url: "https://your-server.example/ota"

backend:
  server:
    public_ws_url: "wss://your-server.example/ws"
